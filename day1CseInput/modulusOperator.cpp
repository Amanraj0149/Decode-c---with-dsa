#include<iostream>
using namespace std;
int main(){
    int a=10;
    int b=4;
    int r=a%b;
    cout<<r<<endl;
    // 📍 if a>b then a%b = reamainder
   cout<<10%4<<endl;
    // 📍if a<b then a%b = a
    cout<<3%6<<endl;
    // 📍 if a%(-b)=a%b  
    // 📍 if (-a)%b= (-a%b )
     // 📍 if (-a)%(-b) = (-a%b ) 
     // 5%2  
}